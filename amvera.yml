meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile?target=all-in-one
  skip: false
run:
  image: null
  command: null
  args: null
  persistenceMount: /var/openproject/pgdata
  containerPort: "8080"